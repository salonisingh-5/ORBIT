---
phase: 2
plan: 2
wave: 2
gap_closure: false
---

# Plan 2.2: Opportunity Feed UI, Category Badges, and Detail Page

## Objective
Construct the student opportunity discovery interface: interactive category badges, search & sort controls, editorial opportunity cards with deadline countdowns, and a comprehensive opportunity detail page at `/opportunities/[slug]`.

## Context
Load these files for context:
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md
- .gsd/phases/2/2.1-PLAN.md
- src/lib/opportunities.ts
- src/app/page.tsx

## Tasks

<task type="auto">
  <name>Build Opportunity Card, Category Filter, and Feed Interface</name>
  <files>
    src/components/opportunities/opportunity-card.tsx
    src/components/opportunities/category-filter.tsx
    src/components/opportunities/opportunity-feed.tsx
    src/app/page.tsx
  </files>
  <action>
    Construct the interactive opportunity browse feed adhering to the editorial beige design system.

    Steps:
    1. Create `src/components/opportunities/opportunity-card.tsx`:
       - Editorial styling: `bg-orbit-card border border-orbit-border hover:border-orbit-gold/60 hover:shadow-md transition-all rounded-xl p-5`.
       - Club byline: Logo/initials pill, club name, and relative date.
       - Category badge with icon and distinct soft tint.
       - Serif opportunity title linking to `/opportunities/[slug]`.
       - Key details: location/mode, deadline countdown badge (e.g. "3 days left", "Deadline: Oct 14"), and registration link CTA.
       - Bookmark button slot with optimistic toggle.
    2. Create `src/components/opportunities/category-filter.tsx`:
       - Horizontal scrollable bar with category pills (All, Hackathons, CTFs, Contests, Workshops, Internships, Competitions).
       - Active state styling: muted gold border and warm paper background.
    3. Create `src/components/opportunities/opportunity-feed.tsx`:
       - Client component managing active search text, selected category, and sort order.
       - Empty state with editorial icon, message, and "Reset filters" button when no results match.
    4. Update `src/app/page.tsx` to render the feed loaded with opportunities from `getOpportunities()`.

    AVOID: Layout shift during filtering; keep feed layout stable with smooth CSS transitions.
    USE: Clean semantic tags and accessible buttons.
  </action>
  <verify>
    npm run build
  </verify>
  <done>
    `npm run build` succeeds; home page renders dynamic opportunity feed with functional search input and category filtering.
  </done>
</task>

<task type="auto">
  <name>Build Dedicated Opportunity Detail Page</name>
  <files>
    src/app/opportunities/[slug]/page.tsx
    src/components/opportunities/opportunity-detail.tsx
  </files>
  <action>
    Develop the full-page editorial opportunity detail view with dates, eligibility, and official registration CTA.

    Steps:
    1. Create `src/components/opportunities/opportunity-detail.tsx`:
       - Header with breadcrumbs ("Opportunities > Category > Title").
       - High-contrast serif heading, organizing club card with contact/website info.
       - Schedule sidebar / key dates widget: Registration Deadline, Start Date, End Date, Location/Venue.
       - Long-form formatted description and eligibility criteria.
       - Prominent editorial CTA button: "Apply / Register on Official Portal" opening external link in new tab.
       - Share and bookmark actions.
    2. Create `src/app/opportunities/[slug]/page.tsx`:
       - App Router dynamic route fetching opportunity via `getOpportunityBySlug(params.slug)`.
       - Returns `notFound()` if opportunity does not exist.
       - Generates dynamic metadata for SEO.

    AVOID: Static 404 pages without a helpful return button.
    USE: Editorial typographic hierarchy with generous whitespace.
  </action>
  <verify>
    npm run build
  </verify>
  <done>
    `npm run build` succeeds; `/opportunities/[slug]` generates as dynamic route and renders complete opportunity details.
  </done>
</task>

## Must-Haves
After all tasks complete, verify:
- [ ] Opportunity feed renders cards with category badges and deadline countdowns
- [ ] Search input and category filters instantly narrow down displayed cards
- [ ] Detail page at `/opportunities/[slug]` renders full schedule, description, and official link
- [ ] External registration links open securely with `rel="noopener noreferrer"`

## Success Criteria
- [ ] All tasks verified passing
- [ ] Responsive across mobile and desktop
- [ ] Editorial beige aesthetic maintained across all opportunity views
