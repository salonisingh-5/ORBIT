---
phase: 2
plan: 1
wave: 1
gap_closure: false
---

# Plan 2.1: Opportunity Data Access Layer, Filtering API, and Seed Opportunities

## Objective
Establish the opportunity data access layer with type-safe queries, realistic RVCE seed opportunities (hackathons, CTFs, contests, workshops, internships), and a REST search & filtering API endpoint.

## Context
Load these files for context:
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md
- .gsd/phases/2/RESEARCH.md
- prisma/schema.prisma
- src/lib/prisma.ts

## Tasks

<task type="auto">
  <name>Create RVCE Seed Data and Database Seeding Script</name>
  <files>
    src/lib/seed-data.ts
    prisma/seed.ts
    package.json
  </files>
  <action>
    Define realistic RVCE clubs and student opportunities with comprehensive metadata, and configure database seeding.

    Steps:
    1. Create `src/lib/seed-data.ts` with:
       - 5 RVCE clubs: Coding Club RVCE, IEEE RVCE Student Branch, Team Astra Robotics, RVCE E-Cell, GDG on Campus RVCE.
       - 8+ diverse opportunities across categories:
         - HACKATHON: "8th Mile National Hackathon 2026" (₹2,00,000 prize pool, 36h hackathon)
         - CTF: "RVCE CyberShield CTF v4" (Binary exploitation, Web, Crypto, Reverse engineering)
         - CODING_CONTEST: "CodeBlitz: Algorithmic Duel 2026" (Speed ICPC-style programming)
         - WORKSHOP: "Hands-on Rust & Systems Engineering Workshop" (2-day technical bootcamp)
         - INTERNSHIP: "Summer AI/ML Research Fellowship @ Center for Imaging & Speech" (Stipend, on-campus)
         - COMPETITION: "VentureX: RVCE Inter-College Startup Pitch Competition"
       - Complete fields: `title`, `slug`, `description`, `category`, `officialUrl`, `deadline`, `startDate`, `endDate`, `location`, `status`, `club`.
    2. Create `prisma/seed.ts` upserting clubs and opportunities into PostgreSQL.
    3. Add `"prisma": { "seed": "node --loader ts-node/esm prisma/seed.ts" }` or equivalent script in `package.json`.

    AVOID: Shallow placeholder strings like "Lorem ipsum". Use realistic campus details and official registration links.
    USE: Explicit ISO date strings and valid category enum values.
  </action>
  <verify>
    node -e "const { SEED_OPPORTUNITIES } = require('./src/lib/seed-data.ts'); console.log('Opportunities count:', SEED_OPPORTUNITIES.length);" || npm run build
  </verify>
  <done>
    `src/lib/seed-data.ts` defines 8+ validated opportunities across all categories; file compiles with TypeScript without type errors.
  </done>
</task>

<task type="auto">
  <name>Build Opportunity Data Access Helpers and Filtering API Handler</name>
  <files>
    src/lib/opportunities.ts
    src/app/api/opportunities/route.ts
  </files>
  <action>
    Implement data access layer and search/filter API handler with database resilience.

    Steps:
    1. Create `src/lib/opportunities.ts`:
       - `getOpportunities(params: { search?: string; category?: string; sortBy?: "deadline" | "newest"; clubId?: string })`
       - Queries `prisma.opportunity.findMany(...)` with Prisma search, category filter, and sorting.
       - If database connection fails (offline local dev), falls back smoothly to filtering `SEED_OPPORTUNITIES` in memory so the app never crashes.
       - `getOpportunityBySlug(slug: string)`: fetches single opportunity with club details.
       - `getOpportunityStats()`: returns count by category and upcoming deadlines.
    2. Create `src/app/api/opportunities/route.ts`:
       - `GET` handler parsing search query parameters `search`, `category`, and `sortBy`.
       - Returns JSON `{ success: true, count: number, opportunities: Opportunity[] }`.

    AVOID: Crashing if PostgreSQL is unreachable during offline local development.
    USE: Dual-mode resilience (Prisma query with graceful fallback to validated seed dataset).
  </action>
  <verify>
    npm run build
  </verify>
  <done>
    `npm run build` succeeds; `/api/opportunities` compiles as a dynamic endpoint and returns formatted opportunity data.
  </done>
</task>

## Must-Haves
After all tasks complete, verify:
- [ ] Rich RVCE seed opportunities exported with complete metadata
- [ ] Data access functions filter by search query, category, and deadline sorting
- [ ] `/api/opportunities` API endpoint returns status 200 with opportunity array

## Success Criteria
- [ ] All tasks verified passing
- [ ] Type check succeeds
- [ ] Data layer ready for Feed UI consumption
